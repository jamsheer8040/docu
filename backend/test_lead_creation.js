const { Lead, LeadStatusHistory } = require("./models"); async function run() { try { const leadPayload = { name: "", company_name: "Test Company", email: "test@example.com", phone: "1234567890", status: "New", source: "Website", notes: "Test note", service_id: 1, tenant_id: 16, created_by: 1 }; const lead = await Lead.create(leadPayload); console.log("Created successfully:", lead.toJSON()); } catch (e) { console.error("Validation Error:", e.message); } process.exit(0); } run();

const { sequelize, User, Role } = require("./models"); const { getBalanceSheet } = require("./controllers/reports.controller.js"); async function run() { const user = await User.findOne({ where: { email: 'ashraf15@gmail.com' }, include: [{ model: Role }] }); const req = { query: {}, user: user }; const res = { json: (data) => console.log(JSON.stringify(data, null, 2)), status: (code) => ({ json: (data) => console.log(code, data) }) }; await getBalanceSheet(req, res); process.exit(0); } run();

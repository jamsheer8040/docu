const { sequelize, User, Role } = require("./models"); const { getServiceTypes } = require("./controllers/service.controller.js"); async function run() { const user = await User.findOne({ where: { email: 'ashraf15@gmail.com' }, include: [{ model: Role }] }); const req = { query: {}, user: user }; const res = { json: (data) => console.log(data), status: (code) => ({ json: (data) => console.log(code, data) }) }; await getServiceTypes(req, res); process.exit(0); } run();
